import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Button } from '@/components/ui/button';
import { GoalProgress } from '@/types/operations';
import { Settings2, Target } from 'lucide-react';
export function ShiftGoalsPanel({ goals, canManage, onConfigure }: { goals: GoalProgress[]; canManage: boolean; onConfigure: () => void }) { return <Card className="rounded-2xl border-slate-800 bg-slate-900"><CardHeader className="p-4 border-b border-slate-800 flex-row justify-between items-center"><CardTitle className="text-sm flex items-center gap-2"><Target className="w-4 h-4 text-emerald-400"/>Shift goals</CardTitle>{canManage && <Button onClick={onConfigure} size="sm" variant="outline" className="border-slate-700 bg-slate-950 text-xs"><Settings2 className="w-3.5 h-3.5 mr-1.5"/>Configure</Button>}</CardHeader><CardContent className="p-4 space-y-4">{goals.map((goal) => <div key={goal.key}><div className="flex justify-between gap-2 text-xs"><span className="font-semibold text-slate-300">{goal.label}</span><span className={goal.state === 'ACHIEVED' ? 'text-emerald-400' : goal.state === 'AT_RISK' ? 'text-amber-400' : 'text-rose-400'}>{goal.actual.toLocaleString()} / {goal.target.toLocaleString()} {goal.unit === 'USD' ? '$' : goal.unit}</span></div><div className="mt-2 h-2 rounded-full bg-slate-800 overflow-hidden"><div className={`h-full rounded-full ${goal.state === 'ACHIEVED' ? 'bg-emerald-500' : goal.state === 'AT_RISK' ? 'bg-amber-400' : 'bg-rose-500'}`} style={{ width: `${goal.progress}%` }}/></div></div>)}</CardContent></Card>; }
