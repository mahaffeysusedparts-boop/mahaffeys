import { Link } from 'react-router-dom';
import { Button } from '@/components/ui/button';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Badge } from '@/components/ui/badge';
import { OperationsAlert } from '@/types/operations';
import { BellRing, Check, Clock3, ExternalLink } from 'lucide-react';
export function OperationsAlertCenter({ alerts, canManage, onAction }: { alerts: OperationsAlert[]; canManage: boolean; onAction: (alert: OperationsAlert, action: 'ACKNOWLEDGED' | 'RESOLVED' | 'SNOOZED') => void }) { const visible = alerts.filter((alert) => alert.status !== 'RESOLVED'); return <Card className="rounded-2xl border-slate-800 bg-slate-900"><CardHeader className="p-4 border-b border-slate-800 flex-row justify-between items-center"><CardTitle className="text-sm flex items-center gap-2"><BellRing className="w-4 h-4 text-amber-400"/>Alert center</CardTitle><Badge className="bg-amber-500/15 text-amber-300 border border-amber-500/30">{visible.length} active</Badge></CardHeader><CardContent className="p-2 space-y-1">{visible.length ? visible.slice(0, 6).map((alert) => <div key={alert.id} className="p-3 rounded-xl bg-slate-950 border border-slate-800"><div className="flex gap-2 items-start"><span className={`mt-1.5 w-2 h-2 rounded-full shrink-0 ${alert.severity === 'URGENT' ? 'bg-rose-400' : 'bg-amber-400'}`}/><div className="min-w-0 flex-1"><p className="text-sm font-bold text-slate-100">{alert.title}</p><p className="mt-1 text-xs text-slate-400">{alert.message}</p><p className="mt-2 text-xs text-emerald-300">{alert.recommendedAction}</p><div className="mt-3 flex flex-wrap gap-2"><Link to={alert.link}><Button size="sm" variant="outline" className="h-7 text-[11px] border-slate-700"><ExternalLink className="w-3 h-3 mr-1"/>Open</Button></Link>{alert.status === 'OPEN' && <Button size="sm" onClick={() => onAction(alert, 'ACKNOWLEDGED')} className="h-7 text-[11px] bg-amber-600 hover:bg-amber-500"><Check className="w-3 h-3 mr-1"/>Acknowledge</Button>}{canManage && <><Button size="sm" onClick={() => onAction(alert, 'SNOOZED')} variant="outline" className="h-7 text-[11px] border-slate-700"><Clock3 className="w-3 h-3 mr-1"/>Snooze</Button><Button size="sm" onClick={() => onAction(alert, 'RESOLVED')} className="h-7 text-[11px] bg-emerald-600 hover:bg-emerald-500">Resolve</Button></>}</div></div></div></div>) : <p className="p-5 text-sm text-slate-500">No active operational alerts.</p>}</CardContent></Card>; }
