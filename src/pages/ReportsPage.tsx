import React, { useMemo, useState } from 'react';
import { Link } from 'react-router-dom';
import { Navbar } from '@/components/layout/Navbar';
import { storageService } from '@/services/storageService';
import { deriveOperationsMetrics, ticketWeight } from '@/services/operationsMetrics';
import { Button } from '@/components/ui/button';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs';
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table';
import { AreaChart, Area, BarChart, Bar, PieChart, Pie, Cell, ResponsiveContainer, XAxis, YAxis, Tooltip } from 'recharts';
import { downloadCsv } from '@/utils/exportUtils';
import { Activity, Download, Printer, TrendingUp, Scale } from 'lucide-react';
const money = (value: number) => value.toLocaleString(undefined, { style: 'currency', currency: 'USD' });
const colors = ['#34d399', '#fbbf24', '#60a5fa', '#f472b6', '#a78bfa'];
export default function ReportsPage() {
  const [range, setRange] = useState('30'); const days = Number(range);
  const { tickets, shipments, vehicles } = useMemo(() => ({ tickets: storageService.getTickets().filter((ticket) => ticket.status === 'COMPLETED'), shipments: storageService.getShipments(), vehicles: storageService.getPullYardVehicles() }), []);
  const since = Date.now() - days * 86400000; const active = tickets.filter((ticket) => new Date(ticket.createdAt).getTime() >= since); const settled = shipments.filter((shipment) => shipment.settlement && new Date(shipment.settlement.settledAt).getTime() >= since);
  const metrics = deriveOperationsMetrics({ tickets: active, shipments: settled, vehicles, openQueueCount: 0, delayedTaskCount: 0 });
  const trend = Array.from({ length: Math.min(days, 30) }, (_, index) => { const date = new Date(Date.now() - (Math.min(days, 30) - 1 - index) * 86400000).toDateString(); return { day: index === Math.min(days, 30) - 1 ? 'Today' : new Date(date).toLocaleDateString(undefined, { month: 'numeric', day: 'numeric' }), payout: active.filter((ticket) => new Date(ticket.createdAt).toDateString() === date).reduce((total, ticket) => total + ticket.finalPayout, 0), revenue: settled.filter((shipment) => new Date(shipment.settlement!.settledAt).toDateString() === date).reduce((total, shipment) => total + shipment.settlement!.amountPaid, 0) }; });
  const cats = Object.entries(active.flatMap((ticket) => ticket.scrapLines || []).reduce<Record<string, number>>((result, line) => ({ ...result, [line.metalCategory]: (result[line.metalCategory] || 0) + line.lineTotal }), {})).map(([name, value]) => ({ name, value }));
  // ── Scale traffic — audit-stamped readings per platform + journal-fed daily LBS ──
  const scaleReadings = useMemo(() => {
    const byName = new Map<string, { name: string; scaleIn: number; scaleOut: number }>();
    active.forEach((ticket) => (ticket.weightTransactions || []).forEach((tx) => {
      const name = tx.scaleName || 'Unstamped';
      const entry = byName.get(name) ?? { name, scaleIn: 0, scaleOut: 0 };
      if (tx.type === 'SCALE_IN') entry.scaleIn += 1; else entry.scaleOut += 1;
      byName.set(name, entry);
    }));
    return [...byName.values()].sort((a, b) => (b.scaleIn + b.scaleOut) - (a.scaleIn + a.scaleOut));
  }, [active]);
  const scaleTraffic = useMemo(() => {
    const events = storageService.getScaleEvents().filter((event) => new Date(event.detectedAt).getTime() >= since);
    return Array.from({ length: Math.min(days, 30) }, (_, index) => {
      const date = new Date(Date.now() - (Math.min(days, 30) - 1 - index) * 86400000).toDateString();
      const dayEvents = events.filter((event) => new Date(event.detectedAt).toDateString() === date);
      return {
        day: index === Math.min(days, 30) - 1 ? 'Today' : new Date(date).toLocaleDateString(undefined, { month: 'numeric', day: 'numeric' }),
        onLbs: dayEvents.filter((e) => e.direction === 'ADDED').reduce((total, e) => total + e.deltaLbs, 0),
        offLbs: dayEvents.filter((e) => e.direction === 'REMOVED').reduce((total, e) => total + e.deltaLbs, 0),
      };
    });
  }, [days, since]);
  const totalLbsOn = scaleTraffic.reduce((total, day) => total + day.onLbs, 0);
  return <div className="min-h-screen bg-slate-950 text-slate-100"><Navbar/><main className="max-w-7xl mx-auto p-4 sm:p-6 space-y-6"><section className="flex flex-col sm:flex-row justify-between gap-4"><div><p className="font-mono text-xs tracking-widest text-emerald-400">BUSINESS INTELLIGENCE</p><h1 className="text-3xl font-black">Reports & analytics</h1><p className="text-slate-400">Shared transaction metrics for management reporting.</p></div><div className="flex gap-2"><Link to="/operations"><Button className="bg-emerald-600 hover:bg-emerald-500"><Activity className="w-4 h-4 mr-2"/>Operations</Button></Link><select value={range} onChange={(event) => setRange(event.target.value)} className="rounded-xl bg-slate-900 border border-slate-700 px-3 text-sm"><option value="1">Today</option><option value="7">7 days</option><option value="30">30 days</option><option value="365">YTD</option></select><Button onClick={() => window.print()} variant="outline" className="border-slate-700"><Printer className="w-4 h-4"/></Button></div></section><div className="grid grid-cols-2 lg:grid-cols-6 gap-3">{[['Tickets', metrics.completedTickets], ['Payouts', money(metrics.payouts)], ['Revenue', money(metrics.shipmentRevenue)], ['Gross margin', money(metrics.grossMargin)], ['Lbs in', active.reduce((total, ticket) => total + ticketWeight(ticket), 0).toLocaleString()], ['Vehicles', metrics.vehiclesProcessed]].map(([label, value]) => <Card key={String(label)} className="bg-slate-900 border-slate-800 rounded-2xl"><CardContent className="p-4"><p className="text-xs text-slate-400">{label}</p><p className="font-mono font-black text-lg mt-2">{value}</p></CardContent></Card>)}</div><Tabs defaultValue="overview"><TabsList className="bg-slate-900"><TabsTrigger value="overview">Overview</TabsTrigger><TabsTrigger value="materials">Material mix</TabsTrigger><TabsTrigger value="scales">Scale traffic</TabsTrigger></TabsList><TabsContent value="overview" className="grid lg:grid-cols-2 gap-5 mt-5"><Card className="bg-slate-900 border-slate-800 rounded-2xl"><CardHeader><CardTitle>Daily payout vs revenue</CardTitle></CardHeader><CardContent className="h-72"><ResponsiveContainer><AreaChart data={trend}><XAxis dataKey="day" stroke="#64748b" fontSize={11}/><YAxis stroke="#64748b" fontSize={11}/><Tooltip/><Area dataKey="payout" stroke="#fbbf24" fill="#fbbf2422"/><Area dataKey="revenue" stroke="#34d399" fill="#34d39922"/></AreaChart></ResponsiveContainer></CardContent></Card><Card className="bg-slate-900 border-slate-800 rounded-2xl"><CardHeader><CardTitle>Ticket volume by type</CardTitle></CardHeader><CardContent className="h-72"><ResponsiveContainer><BarChart data={['CAR_SALVAGE', 'SCRAP_METAL', 'MOBILE_SCRAP'].map((type) => ({ type: type.replace('_', ' '), count: active.filter((ticket) => ticket.ticketType === type).length }))}><XAxis dataKey="type" stroke="#64748b" fontSize={11}/><YAxis stroke="#64748b"/><Tooltip/><Bar dataKey="count" fill="#34d399" radius={[6, 6, 0, 0]}/></BarChart></ResponsiveContainer></CardContent></Card></TabsContent><TabsContent value="materials" className="mt-5"><Card className="bg-slate-900 border-slate-800 rounded-2xl"><CardHeader className="flex-row justify-between"><CardTitle>Payout mix by material</CardTitle><Button onClick={() => downloadCsv('material-summary.csv', cats.map((item) => ({ Material: item.name, Payout: item.value.toFixed(2) })))} className="bg-emerald-600"><Download className="w-4 h-4 mr-2"/>CSV</Button></CardHeader><CardContent className="h-72">{cats.length ? <ResponsiveContainer><PieChart><Pie data={cats} dataKey="value" nameKey="name" innerRadius={55} outerRadius={92}>{cats.map((_, index) => <Cell key={index} fill={colors[index % colors.length]}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer> : <Empty/>}</CardContent></Card></TabsContent><TabsContent value="scales" className="grid lg:grid-cols-2 gap-5 mt-5"><Card className="bg-slate-900 border-slate-800 rounded-2xl"><CardHeader className="flex-row justify-between"><CardTitle className="flex items-center gap-2"><Scale className="w-4 h-4 text-emerald-400"/>Readings by platform</CardTitle><Button onClick={() => downloadCsv('scale-readings.csv', scaleReadings.map((row) => ({ Platform: row.name, 'SCALE_IN readings': row.scaleIn, 'SCALE_OUT readings': row.scaleOut })))} disabled={scaleReadings.length === 0} className="bg-emerald-600"><Download className="w-4 h-4 mr-2"/>CSV</Button></CardHeader><CardContent className="p-0">{scaleReadings.length ? <Table><TableHeader className="bg-slate-950/80"><TableRow className="border-slate-800 hover:bg-slate-950 text-xs"><TableHead className="text-slate-400">Platform</TableHead><TableHead className="text-right text-slate-400">SCALE_IN</TableHead><TableHead className="text-right text-slate-400">SCALE_OUT</TableHead><TableHead className="text-right text-slate-400">Total readings</TableHead></TableRow></TableHeader><TableBody>{scaleReadings.map((row) => <TableRow key={row.name} className="border-slate-800 text-xs font-mono hover:bg-slate-800/40"><TableCell className="font-sans font-semibold text-white">{row.name}</TableCell><TableCell className="text-right text-emerald-400">{row.scaleIn}</TableCell><TableCell className="text-right text-amber-400">{row.scaleOut}</TableCell><TableCell className="text-right font-bold text-white">{row.scaleIn + row.scaleOut}</TableCell></TableRow>)}</TableBody></Table> : <Empty text="Stamped readings appear once weighings record a platform name."/>}</CardContent></Card><Card className="bg-slate-900 border-slate-800 rounded-2xl"><CardHeader><CardTitle className="flex items-center gap-2"><Scale className="w-4 h-4 text-amber-400"/>LBS crossed the platform</CardTitle><p className="text-xs text-slate-400">{totalLbsOn.toLocaleString()} LBS ON across all platforms in this range</p></CardHeader><CardContent className="h-72"><ResponsiveContainer><BarChart data={scaleTraffic}><XAxis dataKey="day" stroke="#64748b" fontSize={11}/><YAxis stroke="#64748b" fontSize={11}/><Tooltip/><Bar dataKey="onLbs" name="LBS ON" stackId="traffic" fill="#34d399" radius={[6, 6, 0, 0]}/><Bar dataKey="offLbs" name="LBS OFF" stackId="traffic" fill="#fbbf24" radius={[6, 6, 0, 0]}/></BarChart></ResponsiveContainer></CardContent></Card></TabsContent></Tabs></main></div>;
}
function Empty({ text }: { text?: string }) { return <div className="h-full flex items-center justify-center text-slate-500 text-sm px-6 py-10 text-center"><TrendingUp className="w-5 h-5 mr-2 shrink-0"/>{text ?? 'Data will appear as transactions are recorded.'}</div>; }
