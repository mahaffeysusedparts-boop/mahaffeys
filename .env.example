# Copy this file to .env before starting the Docker Compose stack.
# Use a long, unique value and do not commit the real password.
POSTGRES_PASSWORD=replace-with-a-long-random-password
APP_PORT=8080
NITRO_COOKIE_SECURE=false
